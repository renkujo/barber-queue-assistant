# Barber Queue — Q Path Refinement Contract R2

Status: **human-selected concept; refinement authorized; no runtime promotion**

## Selected authority

- Concept winner: `Q Path` from `docs/assets/logo-redesign-r1/q-path.svg`.
- Preserve: open queue circuit, deliberate interruption, diagonal forward exit, strong Q recognition, minimal geometry, and one-color reliability.
- Correct: the detached R1 square can read as punctuation or a locator dot instead of a precise cut/continuation.

Do not borrow geometry from `Queue Fold`, `BQ Motion`, the old pastel icon, barber pole, ticket, moustache, scissors, crest, chair, sparkle, or generic SaaS mark.

## R2 design intent

Make the interrupted tail read as one controlled continuation of the same route:

- align the exit to the ring's diagonal flow;
- use a substantial forward segment rather than a detached square;
- keep a visible cut/gap at 16px;
- keep the open counter generous;
- avoid looking like a magnifying glass, map pin, chat bubble, power button, or ordinary typeface Q;
- maintain friendly practical modern character through proportion and calm geometry, not decoration.

## Logo system

Create one refined geometry—not another concept tournament—with these candidate/reference applications:

1. `q-path-r2.svg` — canonical mark, `currentColor`, no backing or wordmark.
2. `q-path-r2-color.svg` — cocoa route with apricot exit; no gradient.
3. `q-path-r2-app-icon.svg` — warm-cream icon application with safe mark placement.
4. `q-path-r2-maskable.svg` — full-bleed warm-cream application with the essential mark inside the central 80% safe zone.
5. `refinement.html` and `refinement.png` — browser-rendered review board.
6. `manifest.md` — provenance, construction logic, QA, limitations, and honest promotion status.

All output stays under `docs/assets/logo-redesign-r2/`.

## Required review board

Show:

- R1 → R2 difference at large size;
- canonical cocoa, reversed white, and two-color application;
- exact `16px`, `32px`, `40px`, `44px`, `180px`, `192px`, and `512px` applications;
- `Barber Queue` header lockup on cream and dark owner-sidebar surface;
- favicon/tab simulation;
- Apple/PWA standard icon and maskable safe-zone simulation;
- monochrome proof with no color-dependent meaning.

## QA

- Browser-render and visually inspect the final board.
- Validate SVG XML and `viewBox` bounds.
- Confirm no clipping and no tiny disconnected residue.
- Confirm R2 remains recognizable at 16px and the gap survives.
- Confirm the app-icon and maskable applications remain centered and safe.
- Record whether R2 still risks reading as a generic Q/magnifier.
- Label everything `selected-concept/refinement-candidate`.

Stop for Kiattisak's human review. Do not change or derive `app/src/app/icon.png`, favicon, Apple/PWA icons, manifest, header, sidebar, production code, or deployed assets in this pass.
